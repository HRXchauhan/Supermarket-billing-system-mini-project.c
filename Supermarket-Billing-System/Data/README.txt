This folder stores runtime data generated by the program:

  products.txt          - product inventory records
  invoice_<n>.txt        - a saved copy of each generated invoice
  invoice_counter.txt   - tracks the next invoice number

These files are created/updated automatically when you run the program
from the SourceCode folder. This folder is intentionally empty until
then.
